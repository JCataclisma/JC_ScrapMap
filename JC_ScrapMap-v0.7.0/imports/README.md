# Captured Maps

This directory starts empty.

`Generate exact roads` stores validated files here as:

`roads-<seed>.json`

Do not upload personal captured files unless you deliberately want to publish
that seed's generated road and water map.

